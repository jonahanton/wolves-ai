# Secrets
.env
.env.worktree

# Python
.venv/
__pycache__/
*.pyc
.pytest_cache/
.ruff_cache/

# Node
node_modules/
.next/
out/
.wrangler/

# OS / editor
.DS_Store
.vscode/
.claude/

# Run artefacts
/runs/

# Purchased odds snapshots: local + S3 only, never git
data/odds/

# Docs
docs/
prototypes/

.terraform/

# Local terraform state (bootstrap keeps its own on disk; never commit it)
terraform.tfstate
terraform.tfstate.*
*.tfstate.backup

.playwright-mcp/
